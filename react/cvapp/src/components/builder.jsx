import {useState} from 'react'

import {Person, Add_details} from './utils.jsx'


export default function Builder({on_add}){

    //const person= new Person();
    //const [info, set_info]=useState(person);


    return(
            <Add_details/>
    )
}